import React,{ Component } from 'react' ;
import Breadcrumb from '../Breadcrumb/index'

export default class Buttons extends Component {
  render () {
    return (
        <Breadcrumb arr={['Button']} />
    )
  }
}