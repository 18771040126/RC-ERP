import React,{ Component } from 'react' ;
import Breadcrumb from '../Breadcrumb/index'

export default class Radios extends Component {
    render () {
        return (
                <Breadcrumb arr={['Radio']} />
        )
    }
}